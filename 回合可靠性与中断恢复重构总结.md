# Reasonix 回合可靠性与中断恢复重构总结

## 背景

Issue #9825 反映长会话中工具调用频繁被宿主中断，导致：
1. 已完成的工具结果丢失（工作已落库但模型上下文未收到）
2. 恢复提示缺少完整参数，无法安全重试
3. 连续中断后体验断崖式下降

## 当前分支实现（已完成）

`fix/interrupted-turn-recovery` 分支已实现核心基础设施：

### 1. 工具执行状态分类
- `ToolRunState`: completed/cancelled/unknown/pending/not_started
- `RecordToolRecovery`: 根据状态分类工具到不同恢复桶
- `ToolResultRunState`: 从工具结果内容推断执行状态

### 2. 写入意图记录与验证
- `WriteIntentHook`: 在工具执行时记录文件写入意图
- `WriteRecoveryCheck`: 后置条件验证（satisfied/unknown/absent）
- `recoverPreviousWrite`: 阻止重复已满足的写入
- `recoverPreviousUnknown`: 阻止未知结果的副作用重试

### 3. 恢复身份追踪
- `recoveryIdentity`: agentID/taskID/turnID/attemptID
- 检查点持久化：恢复身份写入 checkpoint 记录
- Desktop 集成：暴露恢复状态到 UI

### 4. 工具执行起始屏障
- 持久化工具开始执行的时间点
- 区分"取消前未启动"与"启动后中断"

### 5. Provider 转录验证
- 确保恢复决策基于协议正确的对话状态
- 防止基于损坏历史做出不安全的重试

## 三阶段实现计划（待完成）

基于当前基础设施，制定了三阶段交付计划：

### Phase 1: 工具级预算检查点（下一个 PR）

**问题**：当前预算检查在回合边界进行，无法防止工具执行过程中的中断

**方案**：
- 在每个工具派发前检查预算（而非仅在回合开始时）
- 使用 `nearLimit()` 预测：`current + estimatedToolCost >= limit`
- 工具成本估算：bash 10s, edit_file 2s, read_file 1s+4k tokens
- 命中阈值时：
  1. 返回已完成工具的结果
  2. 未启动的工具标记为 `not_started` 并保留完整参数
  3. 优雅结束回合，返回 `taskBudgetPause`

**实现文件**：
- `internal/agent/run_budget.go`: 添加 `nearLimit()` 方法和成本估算
- `internal/agent/tool_executor.go`: 工具循环中添加检查点
- `internal/agent/interrupted_recovery.go`: 记录未启动工具

**预期效果**：阻止大部分"工具执行到一半被打断"的场景

### Phase 2: 中断信号处理（后续 PR）

**问题**：外部信号（SIGINT, timeout, context cancel）导致进程直接终止

**方案**：
- 工具执行包装 defer 恢复：记录 running → unknown 状态
- 持久化恢复 sidecar：`.reasonix/recovery/<session-id>/<turn-id>.json`
- Bash 进度流式：长时间运行时每 5s 发送进度事件
- 优雅终止：收到 SIGINT 先发 SIGTERM 给子进程，2s 后 SIGKILL

**实现文件**：
- `internal/agent/tool_executor.go`: defer 状态持久化
- `internal/agent/recovery_sidecar.go`: sidecar 读写
- `internal/tool/builtin/bash.go`: 进度流与优雅终止

**预期效果**：进程崩溃/强制终止后，下次启动能恢复中断状态

### Phase 3: 已完成结果注入（后续 PR）

**问题**：工具完成但回合中断前模型未收到结果，导致上下文丢失

**方案**：
- 持久化已完成工具的结果到恢复交接
- 下次回合启动时，注入为 `LocalOnly: true` 的 tool result 消息
- 模型看到结果上下文，无需重新执行

**实现文件**：
- `internal/agent/interrupted_recovery.go`: 注入已完成结果
- `internal/agent/session.go`: InsertBeforeRecovery 方法
- Desktop: UI 显示已完成结果

**预期效果**：用户不再因中断丢失已完成的工作

## 文档结构

```
docs/
├── INTERRUPTED_TURN_RECOVERY_DESIGN.md  # 整体设计：已实现 + 待实现
├── TURN_INTERRUPTION_IMPLEMENTATION.md  # 三阶段实现计划（根因、方案、测试）
└── PHASE1_CHECKLIST.md                   # Phase 1 详细任务清单
```

## 当前状态

- ✅ 基础设施完整（状态分类、写入验证、恢复身份、起始屏障）
- ✅ 设计文档与实现计划完成
- ⏳ Phase 1 实现待开始（预计 10-15 小时，约 2 个工作日）
- ⏳ Phase 2/3 后续交付

## 下一步行动

1. **开始 Phase 1 实现**：按 `PHASE1_CHECKLIST.md` 执行
   - 实现 `nearLimit()` 和工具成本估算
   - 在工具循环中添加检查点
   - 编写单元和集成测试
   
2. **手动验证**：
   - 长会话场景（1000+ 工具调用，设置预算压力）
   - 部分批次完成场景（5 个工具，预算只够 3 个）
   - 回归测试（正常回合无延迟增加）

3. **评估效果**：
   - Phase 1 完成后，观察 #9825 场景是否显著改善
   - 如果仍有中断 → 优先 Phase 2（信号处理）
   - 如果大部分解决 → Phase 3 可作为低优先级清理

## 关键决策点

1. **工具成本估算准确性**：当前使用静态保守值，后续可考虑基于历史执行时间动态调整
2. **预算阈值边界**：当前使用固定缓冲（30s, 10k tokens），未来可改为百分比（剩余 10%）
3. **MCP 工具估算**：使用通用 5s 默认值，后续可考虑让 MCP server 声明自己的估算

## 成功标准

- [ ] Issue #9825 报告的场景不再丢失已完成工作
- [ ] 长会话（1000+ 工具）完成时无用户可见的中断
- [ ] 预算超限时优雅返回部分结果
- [ ] 外部中断（SIGINT, timeout）留下可恢复状态
- [ ] 正常（无中断）回合无延迟回归

## 相关资源

- Issue #9825: 回合频繁被宿主中断
- `internal/provider/tool_recovery.go`: 状态分类逻辑
- `internal/agent/write_recovery.go`: 写入意图验证
- `internal/agent/interrupted_recovery.go`: 恢复块格式化
- `docs/OPENCODE_RECOVERY_COMPARISON.md`: OpenCode 分析与建议
