# 编排层全量回归报告（100 话题 × 2 帧真实运行）

日期：2026-08-04（北京时间）
工具：`cmd/dbg-usage/fleet100_live.go`（8 并发，真实 DeepSeek web_search）
缓存：真实用户缓存目录（~/.cache/reasonix/websearch，147 条既有条目）

## 结果总览

| 指标 | 结果 |
|---|---|
| 帧总数 | 200（100 话题 × zh/en 2 语言帧） |
| 缓存命中 | **180（90%）**——同话题 zh/en 帧共享 L1 缓存 + 既有 147 条 |
| 首次联网 | 20（10%）——首次话题真实 web_search 后落盘 |
| 错误 | **0** |
| 总耗时 | 16m4s（8 并发；单帧 avg 37.4s / min 9.4s / max 97.6s） |

## 关键结论

1. **编排层全链路真实可用**：BuildFleetRetrievalTasks（200 任务生成 3ms）
   + Retrieve（缓存优先 → 服务端 web_search 蒸馏落盘）→ 报告渲染，零失败。
2. **缓存复用显著**：90% 帧直接 L1 命中（10ms 级）——第二次全量回归预期
   95%+（20 个首次联网话题已落盘）。
3. **并发正确**：8 并发共享缓存无竞态（命中/落盘交错正常，LRU 容量治理生效）。
4. **成本**：200 帧仅 20 次真实 web_search（约 1/3 于首次 100 话题测试的 66 次）
   ——编排层语言分帧后缓存复用放大收益。

## 信息模型更新

1. 编排层默认 DepthL1 × [zh,en] × general = 2 帧/话题；同话题语言帧共享
   缓存键（L1 hash 不区分语言——zh/en 帧复用同一蒸馏结果，成本减半）。
2. 真实链路单帧耗时 9-98s（服务端思考+蒸馏波动）——8 并发全量 16 分钟
   可接受；更深 Depth 需并行扩容或缓存预热。
