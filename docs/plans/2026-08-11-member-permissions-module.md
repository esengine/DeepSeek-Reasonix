# 成员与权限独立模块实施计划

## Step 1：补齐成员数据契约

- Output：`site/server/platform-store.mjs` 的成员响应包含最近登录时间，且保持工作空间隔离。
- Test：扩展 `site/server/platform-store.test.mjs`，验证有会话、无会话和跨空间成员的结果。

## Step 2：建立独立页面与权限入口

- Output：`site/src/pages/index.astro` 新增“成员与权限”导航、完整页面、系统摘要、“我的权限”和变更确认弹窗。
- Test：静态契约测试验证页面结构、中文业务文案和必要测试标识。

## Step 3：实现页面状态与安全交互

- Output：`site/src/scripts/ip-platform.mjs` 完成管理员路由保护、成员概览、筛选、邀请说明、最近登录、角色/状态确认和“我的权限”。
- Test：脚本单元/契约测试覆盖权限可见性与关键业务文案；API 失败不会显示成功状态。

## Step 4：完成响应式视觉设计

- Output：`site/src/styles/ip-platform.css` 提供桌面与手机布局、清晰状态、键盘焦点和确认弹窗样式。
- Test：构建成功，并在真实 UI 的桌面和手机视口检查无溢出、无重叠、可操作。

## Step 5：更新并执行角色化 E2E

- Output：成员协作、模块巡检和运维测试适配独立模块，新增首次使用关键路径验证。
- Test：所有者邀请/改角色/停用，编辑者和只读成员边界，系统摘要、我的权限、移动端均通过。

## Step 6：真实 UI 验收与交付截图

- Output：重启 4388 真实服务，保存成员概览、角色确认、我的权限和普通成员边界截图及报告。
- Test：截图加载完成且数字、文案、按钮状态与真实 API 一致；最终运行完整测试与构建。

## 执行说明

当前工作区包含此前连续迭代的未提交改动，新模块依赖这些改动，因此在原工作区内谨慎增量实现，不创建隔离 worktree，也不擅自提交或覆盖现有文件。
