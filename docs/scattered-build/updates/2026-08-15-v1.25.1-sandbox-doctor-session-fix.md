# Scattered Build Update Report: CLI project session diagnostics

- 日期：2026-08-15
- 推送类型：散装版源码修复与更新报告
- 源码提交：`8e0742e`
- 上游 `main-v2` 基线：`a88f117a66fe`
- 关联上游 Pull Request：`#8889`，状态为 open，尚未合并。

本报告记录本次公开源码修复，不包含编译二进制、运行数据、API key、会话正文或本机路径。

## 提交记录

### `8e0742e`：修复 CLI 项目级会话统计遗漏

- 问题：CLI `doctor --json` 只统计全局会话目录，桌面工作区产生的项目级 session 未被计入，已有历史的散装环境可能被误报为会话数为零。
- 方案：调整 doctor 的会话发现与统计边界，增加项目会话目录扫描，并分别输出全局会话、项目会话、项目目录和总占用信息。
- 逻辑：读取桌面项目索引解析项目根目录，按同一 session 文件规则聚合全局与项目数据；保持只读诊断语义，不修改会话、凭据或数据库内容。
- 结果：项目级 session 能被 CLI 正确统计；新增回归测试和可复用 Windows 验收脚本通过，修复已推送至自有 fork，并加入上游 Pull Request #8889。

## 验收结果

- `go test ./internal/doctor -count=1`：通过。
- `go test ./internal/cli -count=1`：通过。
- 项目级 session 夹具验收：通过，`doctor --json` 正确区分 global/project session。
- 上游 PR `#8889`：已推送本次提交，等待维护者评审，不能写成已合并。

## 本次推送边界

本次推送只包含源码和公开文档。以下内容不进入 Git：编译 exe、运行时 home/state/cache、SQLite/WAL/SHM、WebView2 profile、日志、真实 provider 配置、API key、会话正文和机器本地绝对路径。
