/**
 * Demo utility module — demonstrates submit_plan with risk:med.
 *
 * This file is part of a live demo and will be cleaned up by step 5.
 */

/** Greet with a formatted message */
export function greet(name: string): string {
  return `Hello, ${name}! Welcome to Reasonix Code.`;
}

/** Add two numbers (with type safety) */
export function add(a: number, b: number): number {
  return a + b;
}
